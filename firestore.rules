rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // All application data is accessed through the authenticated API.
      // Browser and anonymous REST access must never reach Firestore directly.
      allow read, write: if false;
    }
  }
}
